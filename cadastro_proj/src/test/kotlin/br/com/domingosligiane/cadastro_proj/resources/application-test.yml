spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cadastrotest?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  jackson:
    default-property-inclusion: non_ null
